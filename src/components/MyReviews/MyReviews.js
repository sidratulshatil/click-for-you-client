import React from 'react';
import useTitle from '../Hooks/useTitle';

const MyReviews = () => {
    useTitle('My Review')
    return (
        <div>
            <h2>My review</h2>
        </div>
    );
};

export default MyReviews;